# 敦煌项目一键启动说明

## 架构总览

本项目由 **8 个服务** 组成，分为 Docker 服务和 Node 服务两组：

**Docker 服务（5 个，由 `services/docker-compose.yml` 管理）：**

| 服务 | 端口 | 技术栈 | 说明 |
|------|------|--------|------|
| mysql | `127.0.0.1:3308` | MySQL 8.0 | 数据库，首次启动自动初始化表结构和数据 |
| sql-agent | `127.0.0.1:3001` | TypeScript + Express | AI 自然语言转 SQL 智能查询，对接千问大模型 |
| equivalence-service | `127.0.0.1:8000` | Python FastAPI | 敦煌实验等效性计算（气候时段 → 加速实验时长） |
| mcp-tools | `127.0.0.1:3100` | TypeScript + MCP SDK | MCP 工具服务器（数据库 Schema / SQL 执行 / 图表生成） |
| sql-agent-v2 | `127.0.0.1:3002` | Python LangGraph + FastAPI | 新一代智能查询 Agent（Master-Slave 架构，通过 MCP 协议调用工具） |

**Node 服务（3 个，由 `start-all.js` 启动）：**

| 服务 | 目录 | 技术栈 | 说明 |
|------|------|--------|------|
| front_con | `front_con/` | Vue 2 + Element UI | 后台管理面板（默认账号 `admin` / 密码 `666666`） |
| back_con | `back_con/` | Express | 后端 API，连接数据库 |
| WeatherVisualization | `WeatherVisualization/` | Vue 3 + Vite + ECharts + AntV L7 | 大屏数据可视化 |

## 前置条件

- **Docker Desktop** 已安装并运行
- **Node.js** 16+ 已安装
- 三个子项目的依赖已安装（`npm install`）

## 一键启动

在项目根目录运行：

```bash
npm start
```

启动流程：
1. 在 `services/` 目录执行 `docker compose up -d --build`，拉取并启动 mysql、sql-agent、equivalence-service、mcp-tools、sql-agent-v2
2. 等待 3 秒让 Docker 容器就绪
3. 依次启动 WeatherVisualization、front_con、back_con 的 `npm run dev`

启动完成后终端会打印：

```
  Docker services:
    mysql (docker):        127.0.0.1:3308
    sql-agent ui/api:      http://127.0.0.1:3001
    equivalence-service:   http://127.0.0.1:8000
    mcp-tools (MCP):       http://127.0.0.1:3100
    sql-agent-v2 (V2):     http://127.0.0.1:3002

  Node services:
    WeatherVisualization:  http://127.0.0.1:5173
    front_con (admin):     http://127.0.0.1:2023
    back_con (API):        http://127.0.0.1:3000
```

## 停止所有服务

在终端按 `Ctrl + C`，脚本会自动：
1. 终止三个 Node 进程
2. 执行 `docker compose down` 停止所有 Docker 容器

## 数据库

首次启动时，Docker 会自动执行 `services/mysql/init/` 下的 SQL 文件初始化数据库：

| SQL 文件 | 内容 |
|------|------|
| `vue_admin_custom.sql` | 系统管理表（菜单、用户、角色、权限等） |
| `weather_data.sql` | 气象业务表（站点信息 391 条 + 逐小时气象数据 ~23.6 万条） |

数据库连接参数（Navicat 等工具连接时使用）：

| 参数 | 值 |
|------|-----|
| Host | `127.0.0.1` |
| Port | `3308` |
| User | `root` |
| Password | `root` |
| Database | `dunhuang_agent` |
| Charset | `utf8mb4` |

## 故障排查

| 问题 | 解决 |
|------|------|
| Docker 启动失败 | 确认 Docker Desktop 正在运行，检查 `3308`/`3001`/`8000`/`3100`/`3002` 端口未占用 |
| SQL 初始化失败 | 查看容器日志 `docker logs dunhuang-mysql-prod` |
| 前端页面空白 | 检查 `back_con` 是否正常启动，查看终端黄色日志 |
| 等效方案计算失败 | 确认 `equivalence-service` 容器正常运行 |
| V2 智能查询无法使用 | 确认 `mcp-tools` 和 `sql-agent-v2` 容器均正常运行 |
| 数据库中文乱码 | 连接工具字符集选 `utf8mb4`，应用层已配置无需处理 |
